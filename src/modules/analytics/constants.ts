export const HUB_SCREEN_IDS = [2, 14];
export const TAG_SCREEN_IDS = [1, 2, 13, 14];

export enum PlaylistSource {
  INTERNAL = 'internal',
  EXTERNAL = 'external',
}

export enum InteractionTypes {
  Click = 1,
  Share = 2,
  Like = 3,
}
